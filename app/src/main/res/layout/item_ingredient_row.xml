<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="4dp">

    <!-- Нумерация строки -->
    <TextView
        android:id="@+id/tv_index"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1."
        android:textSize="16sp"
        android:paddingEnd="8dp"/>

    <!-- Ввод названия ингредиента -->
    <AutoCompleteTextView
        android:id="@+id/actv_ingredient"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:completionThreshold="1"
        android:textSize="15sp"
        android:hint="@string/ingredient"
        android:backgroundTint="?attr/editTextTint"
        android:inputType="textCapSentences"
        android:padding="8dp"
        tools:ignore="TouchTargetSizeCheck" />

    <!-- Тире между полями -->
    <TextView
        android:id="@+id/tv_dash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="—"
        android:textSize="18sp"
        android:padding="4dp" />

    <!-- Ввод количества -->
    <EditText
        android:id="@+id/et_quantity"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:hint="@string/qty"
        android:backgroundTint="?attr/editTextTint"
        android:textSize="15sp"
        android:inputType="numberDecimal"
        android:padding="8dp"
        tools:ignore="TouchTargetSizeCheck" />

    <!-- Ввод или автоподстановка меры -->
    <AutoCompleteTextView
        android:id="@+id/actv_measure"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:completionThreshold="1"
        android:textSize="15sp"
        android:hint="@string/unit"
        android:backgroundTint="?attr/editTextTint"
        android:inputType="text"
        android:padding="8dp"
        tools:ignore="TouchTargetSizeCheck" />

    <ImageButton
        android:id="@+id/btn_remove_row"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:backgroundTint="#00FFFFFF"
        android:contentDescription="trashcan"
        android:onClick="goHome"
        app:srcCompat="?attr/buttonTrashcan"
        tools:ignore="TouchTargetSizeCheck" />
</LinearLayout>
